import {DisplayLinkDirective} from './display-link.directive';

describe('DisplayLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayLinkDirective();
    expect(directive).toBeTruthy();
  });
});
